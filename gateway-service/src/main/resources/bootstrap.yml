spring:
  application:
      name: gateway-service
  output:
      ansi:
        enabled: always #多彩log
  cloud:
    config:
      uri: http://localhost:8089/
      name: gateway-service
      profile: dev
      label: master